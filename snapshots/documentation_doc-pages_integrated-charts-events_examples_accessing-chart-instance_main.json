{
  "eventHandlers": [
    {
      "name": "chartCreated",
      "handlerName": "onChartCreated",
      "handler": "function onChartCreated(event) {console.log('Created chart with ID ' + event.chartId);const chartRef = this.gridApi.getChartRef(event.chartId);chart = chartRef.chart;updateTitle(this.gridApi, chart);}"
    },
    {
      "name": "chartRangeSelectionChanged",
      "handlerName": "onChartRangeSelectionChanged",
      "handler": "function onChartRangeSelectionChanged(event) {console.log('Changed range selection of chart with ID ' + event.chartId);updateTitle(this.gridApi, chart);}"
    },
    {
      "name": "chartDestroyed",
      "handlerName": "onChartDestroyed",
      "handler": "function onChartDestroyed(event) {console.log('Destroyed chart with ID ' + event.chartId);chart = null;}"
    }
  ],
  "properties": [
    {
      "name": "columnDefs",
      "value": "[{ field: 'Month', width: 150, chartDataType: 'category' },{ field: 'Sunshine (hours)', chartDataType: 'series' },{ field: 'Rainfall (mm)', chartDataType: 'series' },]"
    },
    {
      "name": "defaultColDef",
      "value": "{editable: true,sortable: true,flex: 1,minWidth: 100,filter: true,resizable: true,}"
    },
    { "name": "popupParent", "value": "document.body" },
    { "name": "enableRangeSelection", "value": "true" },
    { "name": "enableCharts", "value": "true" }
  ],
  "components": [],
  "defaultColDef": "{editable: true,sortable: true,flex: 1,minWidth: 100,filter: true,resizable: true,}",
  "globalComponents": [],
  "parsedColDefs": "[{field: 'Month',width: 150,chartDataType: 'category' },{field: 'Sunshine (hours)',chartDataType: 'series' },{field: 'Rainfall (mm)',chartDataType: 'series' }]",
  "instanceMethods": [],
  "externalEventHandlers": [],
  "utils": [
    "var chart = null;",
    "function updateTitle(api, chart) {var cellRange = api.getCellRanges()[1];if (!cellRange)return;var columnCount = cellRange.columns.length;var rowCount = cellRange.endRow.rowIndex - cellRange.startRow.rowIndex + 1;chart.title.enabled = true;chart.title.text = 'Monthly Weather';chart.subtitle.enabled = true;chart.subtitle.text ='Using series data from ' +columnCount +' column(s) and ' +rowCount +' row(s)';}"
  ],
  "callbackDependencies": {
    "onChartCreated": ["gridOptions", "chart", "updateTitle"],
    "onChartRangeSelectionChanged": ["updateTitle"],
    "onChartDestroyed": ["chart"]
  },
  "data": {
    "url": "'https://www.ag-grid.com/example-assets/weather-se-england.json'",
    "callback": "{params.api.setRowData(data);}"
  },
  "template": "GRID_TEMPLATE_PLACEHOLDER",
  "gridSettings": {
    "width": "100%",
    "height": "100%",
    "theme": "ag-theme-alpine",
    "enterprise": true
  }
}
